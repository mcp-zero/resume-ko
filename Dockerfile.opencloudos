# 使用多阶段构建
# 第一阶段：使用golang官方镜像构建应用程序
FROM golang:1.21-alpine AS builder

# 设置工作目录
WORKDIR /app

# 复制go mod和sum文件
COPY go.mod go.sum ./

# 下载依赖
RUN go mod download

# 复制源代码
COPY . .

# 构建应用程序
RUN CGO_ENABLED=0 GOOS=linux go build -o resume_optimizer

# 第二阶段：使用CentOS 7作为基础镜像
FROM centos:7

# 设置工作目录
WORKDIR /app

# 从builder阶段复制构建好的应用程序
COPY --from=builder /app/resume_optimizer .

# 暴露端口8168
EXPOSE 8168

# 设置入口点
ENTRYPOINT ["./resume_optimizer", "-web", "8168"]